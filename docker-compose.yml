version: "3.8"

services:
  app:
    image: logitrack:latest
    container_name: logitrack-app
    ports:
      - "9876:9876"
    environment:
      - NODE_ENV=production
      - PORT=9876
    restart: unless-stopped
